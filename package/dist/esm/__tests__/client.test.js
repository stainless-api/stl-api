import { createClient } from "../stl";
describe('client', () => {
    it('should test something', () => {
        expect(123).toBe('123');
    });
    it('createClient', () => {
        const client = createClient('/api');
    });
});
//# sourceMappingURL=client.test.js.map